using UnityEngine;
using System.Collections;
using System;
using System.Collections.Generic;
using System.Linq;

public class Test : MonoBehaviour {

	// Use this for initialization
	void Start () {
//		var thing = new List<int> () { 0, 1, 2, 3, 4, 5, 6 };
//		var other = thing.Take (50).ToList();
//		print (other.Count);
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
